import { FilterMetodologiaPipe } from './filter-metodologia.pipe';

describe('FilterMetodologiaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMetodologiaPipe();
    expect(pipe).toBeTruthy();
  });
});
