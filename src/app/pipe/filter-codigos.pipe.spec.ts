import { FilterCodigosPipe } from './filter-codigos.pipe';

describe('FilterCodigosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCodigosPipe();
    expect(pipe).toBeTruthy();
  });
});
